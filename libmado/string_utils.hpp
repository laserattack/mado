#ifndef MADO_STRING_UTILS_HPP
#define MADO_STRING_UTILS_HPP

#include <iostream>
#include <string>

void trim(std::string &s);
void print_json_string(const std::string &str, std::ostream &os);

#endif // MADO_STRING_UTILS_HPP
